// Game Over JS file. Contains the GameOver state

/*global Phaser, game*/

var GameOver = {
init : function() {
    
    // Initialize data
    
    console.log("-> GameOver init");
},

preload : function() {

    // Load resources
    
},

create : function() {
    
    // Create objects (sprites, etc.)
},

update : function(dt) {
    
    // Update logic. Called once per frame. dt = time between frames
},

render : function() {
    
    // Custom rendering operations
},
}; // GameOver var